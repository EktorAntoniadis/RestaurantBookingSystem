@page
@model RestaurantBookingSystem.App.Pages.Administration.AddPermissionModel
@{
    Layout = "_AdminLayout";
}

@section AdminSideBar {
    <partial name="Administration/_AdminSidebar" />
}

<h1> Add permission </h1>
<form method="post" asp-page-handler="AddNewPermission">
    <div class="mt-4">
        <div class="row">
            <div class="col-6">
                <div class="form-group mt-3">
                    <label class="form-label" asp-for="NewPermission.Name">Name</label>
                    <input class="form-control" type="text" asp-for="NewPermission.Name" value="@Model.NewPermission.Name" />
                </div>
                <div class="form-group mt-3">
                    <label class="form-label" asp-for="NewPermission.Description">Description</label>
                    <textarea asp-for="NewPermission.Description" class="form-control"></textarea>
                </div>
            </div>
        </div>
        <div class="form-group mt-3">
            <a asp-page="/Administration/Index" class="btn btn-secondary">Back</a>
            <button type="submit" class="btn btn-primary">Add New Permission</button>
        </div>
    </div>
</form>